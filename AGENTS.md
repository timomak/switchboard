# Switchboard

This repository is Switchboard, not LoseStreak. Never modify LoseStreak or the original ai-usagebar checkout as part of work here.

Preserve upstream licenses, third-party notices, compatible executable/storage paths and existing account data. Do not publish credentials, local account stores, raw diagnostics or personal screenshots. Use synthetic fixtures for automated tests.

The user confirms switching has been tested. Do not label the project "in development" or require repeating all prior live tests before ordinary source changes. Document concrete behavioral limitations accurately. The current bundle script still enables the preview guard; treat changing it as a deliberate implementation change, not a documentation workaround.

Use focused checks appropriate to the diff. No inherited AUR/crates publication. Signing and notarization use local Keychain credentials; never put secrets in source or workflow files. Work in branches for follow-up changes and preserve unrelated work.
